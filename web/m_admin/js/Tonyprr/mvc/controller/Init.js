Ext.define('Tonyprr.mvc.controller.Init', {
    extend	: 'Ext.app.Controller',
    views	: [
            'Tonyprr.mvc.view.WinReset'
    ],

    init	: function(app) {
        this.callParent(null);
        this.control({
        });
    }
    
});